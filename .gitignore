__pycache__/
*.wav
# committed test fixture audio (tiny placeholder)
!.claude/skills/wiz-modifier/tests/fixtures/*.wav

# Environment variable files — never commit API keys
.env
.env.*
!.env.example

# WIZ manuals and decoded extraction scratch — never committed
docs/personal/*.pdf
*_dec.txt
/tmp/product*.txt
/tmp/config*.txt
# PDF extraction scratch — never committed
.wizscratch/

# Python build artifacts
*.egg-info/

# Docs, specs, design notes, manuals — never committed (kept on disk only)
docs/

# Real talkbot exports / corpus (customer dialogue JSON + audio) — never committed
talkbot/

# Dashboard session persistence — local runtime state
dashboard/backend/.session/
dashboard/backend/.sessions/
