schema_version: 1
source_manuals:
  product: { title: "Talkbot Product Guidance", version: "unknown" }
  config:  { title: "Talkbot Configuration Guidance", version: "unknown" }
facts:
  - id: pause.default_ms
    value: 400
    cite: { manual: config, pages: [21] }
    quote: "The pause duration is 400ms by default"
    confidence: documented
    note: "Pause after the node finishes speaking, before the chasing statement (config 1.5.x Pause Duration)."
  - id: sentence_segmentation_threshold.default_ms
    value: 300
    cite: { manual: config, pages: [23] }
    quote: "segmentation threshold (default: 300ms)"
    confidence: documented
    note: "If the pause exceeds this threshold, speech is segmented (config 1.5.5 Sentence Segmentation Threshold)."
