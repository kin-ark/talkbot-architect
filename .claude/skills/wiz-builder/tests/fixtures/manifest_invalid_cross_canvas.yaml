name: "Bad Bot"
branch: dev
language: IDN
canvases:
  - name: "1. Greeting"
    nodes:
      - id: greet-root
        prompt: Greeting
  - name: "2. Pitch"
    nodes:
      - id: pitch-child
        prompt: Pitch
    edges:
      # cross-canvas ref — greet-root is in canvas "1. Greeting", not here
      - {from: pitch-child, branch: Unclassified, to: greet-root}
